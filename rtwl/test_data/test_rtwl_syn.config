# config file for rtwl
# parameters are defined by the syntax
# par_name = par_value

# Note boolean values can be either .true. or .false.

offline       = .true.
sta_list      = FJS,FLR,HDL,RVL,SNE,UV01,UV02,UV03,UV04,UV05,UV06,UV07,UV08,UV09,UV10,UV11,UV12,UV13,UV14,UV15
dt            = 0.01
n_regions     = 1
 
base_path     = test_data 
outdir        = SynTest
datadir       = RealDataTest
data_glob     = YA*MSEED
time_grid     = Slow_len.100m.P

max_length    = 300
safety_margin = 20

syn           = .true.
syn_npts      = 50

filt_f0       = 27.0
filt_sigma    = 7.0
kwin          = 3.0
    
